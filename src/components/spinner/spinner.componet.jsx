import { SpinnerContainer, SpinnerOverlay } from "./spinner.style";
const Spinner = () => {
  console.log(true);
  return (
    <SpinnerOverlay>
      <SpinnerContainer />
    </SpinnerOverlay>
  );
};

export default Spinner;
